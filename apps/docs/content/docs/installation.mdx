---
title: Installation
description: Installation instructions for @polgubau/fuzzy
---

## Installation

Install the library using your package manager of choice:

<Tabs groupId="language" items={['npm','yarn','pnpm','bun']} persist defaultIndex="0">
  <Tab value="npm">
  ```sh
  npm install @polgubau/fuzzy
  ```
  </Tab>
  <Tab value="yarn">
  ```sh
  yarn add @polgubau/fuzzy
  ```
  </Tab>
  <Tab value="pnpm">
  ```sh
  pnpm add @polgubau/fuzzy
  ```
  </Tab>
  <Tab value="bun">
  ```sh
  bun add @polgubau/fuzzy
  ```
  </Tab>
</Tabs>


## Getting Started

Import the library and use it with a simple list:

```ts
import fuzzy from '@polgubau/fuzzy';

const list = ["apple", "banana", "orange", "grape"];
const queryText = "appl"; // The search term
const fuzzySearch = fuzzy(list);

// Run this whenever the search term changes
const results = fuzzySearch(queryText);

console.log(results); 
/* Example output: [{ item: "apple", score: 0.9, matches: [0,3] }] */
```

## How It Works

- **Initialize the fuzzy finder** with your list of items.
- **Call the returned function** with a search term to get ranked results.
- **Each result includes** the matched item, its score, and matched positions.